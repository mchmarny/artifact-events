steps:

  - name: docker.io/anchore/grype
    entrypoint: /bin/bash
    args:
    - -c
    - |
      grype --scope AllLayers --output json --file grype.json ${_DIGEST_}

  - name: docker.io/aquasec/trivy
    entrypoint: /bin/bash
    args:
    - -c
    - |
      trivy image --format json --output trivy.json ${_DIGEST_}
